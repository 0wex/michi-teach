VITE_CONVEX_URL=https://accurate-bloodhound-858.convex.cloud

# --- Convex deployment env vars (setear con `npx convex env set ...`) ---
# Requeridas por el backend Convex (backend/convex/*):
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
SITE_URL=

# Requerida por la ingesta RAG (Tavily) — se usa desde backend/convex/lib/tavily.ts
TAVILY_API_KEY=
